/**
 * Feature modules live here (domain UI + hooks).
 * Keep route pages thin — compose from these features.
 *
 * Planned:
 *   accounting/   contracts/   properties/   receipts/
 *   reports/      parties/     settings/
 */
export {};
